---
description: Wrap up the session by appending non-obvious lessons to the touched package's INSIGHTS.md
---

Review this session for non-obvious lessons — a bug's real root cause, a workaround, a decision that would look wrong without context. Apply the `engineering-insights` skill: for each package touched, append a short, concrete entry to its `INSIGHTS.md` under the fitting section, checking first for a conflicting or duplicate entry to fix instead. Skip if nothing meets the bar. Report which files you updated, if any.
